#include <stdio.h>
#include "aire.h"

int main(int argc, const char * argv[]) {

    double resultat;
    resultat = aireRectangle(10.0, 20.00);

    printf("L'aire du rectangle est %f\n", resultat);
    return 0;
}